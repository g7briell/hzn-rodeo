const { createClient } = require('@supabase/supabase-js'); const s = createClient(process.env.VITE_SUPABASE_URL || 'YOUR_URL', process.env.VITE_SUPABASE_ANON_KEY || 'YOUR_KEY'); s.from('eventos_oficiais').select('*').limit(5).then(({data}) => { data.forEach(e => { if (e.detalhes && e.detalhes.sorteio) console.log('Found sorteio:', JSON.stringify(e.detalhes.sorteio).substring(0, 200)); else if (e.detalhes && e.detalhes.sorteios) console.log('Found sorteios:', JSON.stringify(e.detalhes.sorteios).substring(0, 200)); else console.log('No sorteio in', e.nome); }); });
